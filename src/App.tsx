function App() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center gap-3 text-zinc-800 dark:text-zinc-100">
      <h1 className="text-3xl font-semibold">Guitar FX Playground</h1>
      <p className="text-sm text-zinc-500">
        Setup OK — Vite + React + TS + Tailwind + Tone.js + dnd-kit
      </p>
    </main>
  )
}

export default App
